<% Event.statuses.each do |key, value| %>
  <% next if current_user.Student? && value.zero? %>
  <%= button_tag t(".#{publish_status?(key) ? "enrolling" : key}"),
    class: "#{rescue_publish_status value}" %>
<% end %>
