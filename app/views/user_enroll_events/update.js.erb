$(`#participant_<%= @user.id %>`).replaceWith(`<%= j render "participant",
  user: @user, user_event: @user_event, sender: current_user %>`);
$(`#participant_<%= @user.id %> ~ hr:first`).remove();
$(`#event_participants`).html(`<%= j render "event_participants",
  event: @user_event %>`);
